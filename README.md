## Running Tests with Coverage (pytest + pytest-cov)

### 1️⃣ Install Dependencies
Make sure you have Python 3.8+ installed.  
Then install the required testing tools:
```bash
pip install pytest pytest-cov
python coverage_run.py
